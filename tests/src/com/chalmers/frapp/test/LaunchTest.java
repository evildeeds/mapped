package com.chalmers.frapp.test;

import android.test.AndroidTestCase;

public class LaunchTest extends AndroidTestCase {

	/**
	 * Check that FRAPP is considered a map application.
	 * :ISSUES:
	 * "Starting" using api v10.
	 * Not actually starting by verify that FRAPP belong to the
	 * list of suggested applications to find a location.
	 */
	/*
	public final void testLaunch() {
		Intent intent =  new Intent(Intent.ACTION_MAIN);
		intent.addCategory("android.intent.category.APP_MAPS");
		//getContext().startActivity(intent);
	}
	*/
}
